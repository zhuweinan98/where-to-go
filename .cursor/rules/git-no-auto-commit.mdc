---
description: 未经用户明确授权禁止 git commit / push 等写入历史与远端
alwaysApply: true
---

# Git：禁止擅自提交与推送

## 原则

- **默认不得**修改 Git 历史或远端：未经用户在本轮对话中**明确授权**，禁止执行 **`git commit`**、**`git push`**、**`git merge`**（产生提交时）、**`git rebase`**、**`git cherry-pick`**、**`git tag`**（附推送意图）、**`git reset --hard`**、**删除分支** 等会改变仓库状态或远端的操作。
- **「改完代码」≠「用户要提交」**：收尾时只可总结改动；若要提交，须**等用户说出**要提交（见下文明示用语）。

## 视为已授权的说法（示例）

用户自然语言中包含类似意图即可，例如：**提交**、**commit**、**帮我提交**、**push**、**推到远端**、**合并进 main 并提交** 等。仅说「保存」**不算**授权提交，除非同时表达要 git 提交。

## 未授权时允许的操作（只读 / 本地诊断）

- **`git status`**、**`git diff`**、**`git log`**、**`git show`** 等只读或诊断用途。
- 需要提交时：**只输出建议命令**，由用户自行执行；除非用户已按上文明确授权。

## 与「执行任务」的关系

- 用户规则要求「不要只告诉用户去跑命令、要自己执行」——**对 Git 写入不适用**：**commit / push 以用户明确授权为准**，避免替用户做决定。
