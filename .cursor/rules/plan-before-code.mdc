---
description: 先给出方案与改动范围，经用户明确认可后再改代码
alwaysApply: true
---

# 先方案后实现

## 原则

- **默认流程**：对涉及多文件、新行为、架构取舍或用户未逐条指定的实现，**先输出方案**（目标、步骤、影响文件、风险/回滚），**待用户明确回复认可**（如「按这个做」「可以改」）后，再修改仓库代码。
- **例外**：用户已写清「按某方案直接实现」、或仅为**单处显而易见的小修补**（错别字、明显笔误、与上文方案完全一致的微调），可直接改，但仍在回复里**用一两句说明改了什么**。
- **禁止**：在用户仅问「怎么做」「给方案」或未表态认可时，**主动大改**或批量改文件「抢跑」。

## 再次提问 / 方案迭代

- 用户**追问、改需求、纠偏、说「应该是…」而未明确说「按方案改代码 / 可以实现了」**时：**只修订文字方案**（目标、边界、落点文件、接口形态），**不得修改**业务代码与测试（`.py` / `index.html` 等实现层）；除非用户本次话里**明确要求**改的是 **Cursor 规则 `.mdc` 本身**（如「把某条写进规则」），可只改对应规则文件。
- 修订后需**完整或增量说明「与上一版方案差在哪」**，便于你确认后再进入实现。

## 与用户沟通的边界

- 若方案有 **2 种及以上** 可选路径，用简短对比（利弊、工作量）列出，**由用户选**后再动代码。
- 涉及 **Git 提交/推送** 仍遵守 `.cursor/rules/git-no-auto-commit.mdc`，不因本规则而默认可提交。
